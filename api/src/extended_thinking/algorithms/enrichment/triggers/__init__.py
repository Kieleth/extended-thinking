"""Enrichment trigger plugins (ADR 011 v2).

MVP trigger (lands in Phase B.2): frequency_threshold. Others
(cluster_formed, rising_activation, on_wisdom, scheduled) land as
consumers ask for them — the plugin family pattern makes each a
self-contained addition.
"""
